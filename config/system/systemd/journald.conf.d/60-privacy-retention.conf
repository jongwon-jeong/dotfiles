[Journal]
# Keep recent diagnostics available without retaining long-lived system metadata.
MaxRetentionSec=1day
# Daily files let the retention policy remove archived data promptly instead of
# waiting for a large active journal file to rotate.
MaxFileSec=1day
